TODO:

+ Create WndProc that polls for window status
  + When minimized -> hide
+ Create Shell_NotifyIcon that
  * That has a "Show" command that re-shows the window
  * That can be shut down (and then simply shows the window)
* Shut down when the process shuts down
